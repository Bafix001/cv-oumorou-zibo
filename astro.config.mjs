import { defineConfig } from "astro/config";
import tailwind from "@astrojs/tailwind";
import alpinejs from "@astrojs/alpinejs";

// Supprimez une des importations de defineConfig
// Vous n'avez besoin que d'une seule déclaration d'importation de defineConfig

export default defineConfig({
  site: "https://Bafix001.github.io",
});
